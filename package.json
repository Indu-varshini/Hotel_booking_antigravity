{
  "name": "hotel-booking-antigravity",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "if [ -d \"frontend\" ]; then cd frontend && npm install && npm run build; else npm install && npm run build; fi"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.2"
  }
}
